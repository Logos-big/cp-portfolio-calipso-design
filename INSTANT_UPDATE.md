# ⚡ Мгновенное обновление из Figma

## 🚀 Быстрый способ (1 клик):

**Дважды кликните на файл:** `update.bat`

Или

**Дважды кликните на файл:** `обновить-сайт.bat`

---

## 📋 Что делает скрипт:

1. ✅ Получает свежие данные из Figma API
2. ✅ Генерирует HTML строго по макету "Main"
3. ✅ Обновляет CSS переменные
4. ✅ Автоматически отправляет в GitHub
5. ✅ Сайт обновляется через 1-2 минуты

---

## ⚠️ Если ошибка 429 (Rate limit):

- Подождите **1-2 минуты**
- Попробуйте снова
- Скрипт автоматически использует кэш, если API недоступен

---

## 🔧 Альтернативный способ (через PowerShell):

Откройте PowerShell в папке проекта:

```powershell
.\scripts\auto-update.ps1
```

---

## 📝 Ручное обновление (если скрипты не работают):

```powershell
# 1. Синхронизация из Figma
.\scripts\figma-sync.ps1

# 2. Генерация HTML
.\scripts\generate-html-from-figma.ps1

# 3. Отправка в GitHub
git add .
git commit -m "Update from Figma"
git push origin main
```

---

**После обновления макета в Figma просто запустите `update.bat`!**

