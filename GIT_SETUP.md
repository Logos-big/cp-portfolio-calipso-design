# Инструкция по подключению проекта к GitHub

## Первый шаг - Инициализация Git репозитория

### 1. Откройте терминал в папке проекта

### 2. Инициализируйте Git репозиторий:
```bash
git init
```

### 3. Добавьте все файлы в staging:
```bash
git add .
```

### 4. Создайте первый коммит:
```bash
git commit -m "Initial commit"
```

## Второй шаг - Создание репозитория на GitHub

### 1. Зайдите на [GitHub.com](https://github.com) и войдите в аккаунт

### 2. Нажмите кнопку "+" в правом верхнем углу → "New repository"

### 3. Заполните форму:
   - **Repository name**: `cp-portfolio-calipso-design` (или любое другое имя)
   - **Description**: Описание проекта (опционально)
   - **Visibility**: Public или Private
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)

### 4. Нажмите "Create repository"

## Третий шаг - Подключение локального репозитория к GitHub

### 1. Скопируйте URL вашего репозитория с GitHub (HTTPS или SSH)

### 2. Добавьте remote:
```bash
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
```

Например:
```bash
git remote add origin https://github.com/username/cp-portfolio-calipso-design.git
```

### 3. Переименуйте ветку в main (если нужно):
```bash
git branch -M main
```

### 4. Отправьте код на GitHub:
```bash
git push -u origin main
```

## Готово! 🎉

Ваш проект теперь подключен к GitHub. В дальнейшем для отправки изменений используйте:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## Полезные команды:

- `git status` - проверить статус файлов
- `git log` - посмотреть историю коммитов
- `git remote -v` - проверить подключенные remote репозитории

